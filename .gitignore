# IDEA
.idea

# MCPs
.mcp.json

# App
node_modules
dist
dist-ssr
*.local
.DS_Store

# Playwright MCP test artifacts
.playwright-mcp

# Claude (local-machine settings only — skills/agents under .claude/ are versioned)
.claude/settings.local.json
